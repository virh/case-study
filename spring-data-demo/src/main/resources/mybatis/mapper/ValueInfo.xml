<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ranhfun.data.mybatisdemo.mapper.ValueInfoMapper" >
    <resultMap id="BaseResultMap" type="com.ranhfun.data.mybatisdemo.model.ValueInfo" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="value" property="value" jdbcType="INTEGER" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, value
    </sql>

    <select id="findAll" resultMap="BaseResultMap"  >
       SELECT 
       <include refid="Base_Column_List" />
       FROM value_info
    </select>

    <select id="findById" parameterType="java.lang.Long" resultMap="BaseResultMap" >
        SELECT 
       <include refid="Base_Column_List" />
       FROM value_info
       WHERE id = #{id}
    </select>

    <delete id="deleteById" parameterType="java.lang.Long" >
       DELETE FROM
             value_info 
       WHERE 
             id = #{id}
    </delete>

    <insert id="insert" parameterType="com.ranhfun.data.mybatisdemo.model.ValueInfo" >
       INSERT INTO 
            value_info
            (id, value) 
        VALUES
            (#{id}, #{value})
    </insert>

    <update id="update" parameterType="com.ranhfun.data.mybatisdemo.model.ValueInfo" >
       UPDATE 
            value_info 
       SET 
       value = #{value}
       WHERE 
            id = #{id}
    </update>

    <delete id="deleteAll">
       DELETE FROM
             value_info 
    </delete>

</mapper>